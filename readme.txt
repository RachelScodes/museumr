Style:
	* Use // ~~~~ END SETUP to separate the 'requiring'
		sections from the rest of the code
	*

To Do:
	[ ] Fix models
		[√] pluralize model js file names
			- Seed links to artists.js, for example
			- Could just change seed.js instead
		[ ] change model schema "years" and "birthYears"
			from String to Integer
	[ ]
	[ ]
